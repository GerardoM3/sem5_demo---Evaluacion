<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.gm3_developer.sem5_demo">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Evaluación_Semana_8"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sem5_demo"

        tools:targetApi="30">
        <activity
            android:name=".MainActivity"
            android:configChanges="screenSize|orientation"
            android:exported="true"
            android:windowSoftInputMode="stateHidden"
            android:theme="@style/Theme.Sem5_demo.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ConsultaSpinner"/>
        <activity
            android:configChanges="screenSize|orientation"
            android:windowSoftInputMode="stateHidden"
            android:name=".ListViewArticulos"
            android:exported="true"
            tools:ignore="Instantiable">

        </activity>

        <activity android:name=".DetalleArticulos"></activity>

    </application>

</manifest>